# Archivos que el escáner de privacidad omite.
# Globs, uno por línea. Se comparan contra la ruta y contra el nombre.
#
# Solo va acá lo que contiene cadenas con FORMA de dato personal pero que
# demostrablemente no lo son: casos de prueba y ejemplos de documentación.
# Nunca agregues acá un archivo con datos reales — el punto del escáner es
# encontrarlos.

# Tests: contienen montos de prueba con formato de miles, que tienen la
# misma forma que una cédula.
engine/tests/*.py

# Docstring del parser de montos: documenta cada formato con un ejemplo.
engine/adapters/generico.py

# Golden master: montos en pesos de las personas ficticias, sin separadores
# de miles porque JSON no los admite en un número. Un monto de nueve dígitos
# sin separador tiene exactamente la forma de una cédula, y el escáner no lo
# puede distinguir sin contexto — con razón.
#
# Se DERIVA de benchmark/personas.py, que está inventariado y no es un
# archivo que alguien edite a mano: si un dato real llegara acá, tendría que
# haber entrado antes por personas.py, que sí se escanea. Y su contenido
# entra al inventario como cualquier otro excluido: nada queda sin revisar.
benchmark/golden.json

# Documentación del propio escáner: muestra ejemplos de lo que detecta.
skills/renta-privacidad/SKILL.md
scripts/escanear_privacidad.py

# Este mismo archivo.
.privacidadignore

# Inventario de lo anterior. Es un REPORTE de hallazgos enmascarados, así
# que sus líneas tienen la forma de lo que reporta. No es un archivo
# fuente: cada línea suya corresponde a una línea ya inventariada de los
# archivos de arriba. Ver test_lo_excluido_esta_inventariado_y_no_crece_solo.
scripts/privacidad-esperado.txt
